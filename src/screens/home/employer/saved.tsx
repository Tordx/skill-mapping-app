import { View, Text } from 'react-native'
import React from 'react'
import { styles } from '../../../styles'

type Props = {}

const EmployerSaved = (props: Props) => {
  return (
    <View style = {styles.container}>
      <Text>EmployerSaved</Text>
    </View>
  )
}

export default EmployerSaved